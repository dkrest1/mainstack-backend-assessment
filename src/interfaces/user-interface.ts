export interface UpdateUserRequest {
    name?: string;
    email?: string;
    username?: string;
    address?: string;
    phone?: string;
}

export interface FileData {
    data: string;
    title: string;
}

